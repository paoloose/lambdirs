{
  "name": "health",
  "description": "health lambda handler",
  "version": "1.0.0-beta.4",
  "main": "handler.ts",
  "scripts": {
    "prebuild": "mkdir -p dist",
    "build": "bun build handler.ts --outfile dist/lambda.mjs"
  },
  "author": "paoloose",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147"
  }
}
